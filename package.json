{
  "name": "kakaobank_organization",
  "version": "0.0.1",
  "scripts": {
    "dev:start": "serve src -p 3000",
    "dev": "concurrently 'npm:dev:start' 'npm:server'",
    "server": "json-server -p 3001 mockup.json"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "serve": "^14.2.4"
  }
}
